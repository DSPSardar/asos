# marketing/.env.example
# Copy to marketing/.env and fill in. Never commit the real .env or a real API key.

# ── ANTHROPIC (Claude AI) ─────────────────────
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# Override the default model (see pipeline/config.js for why the default is claude-sonnet-5)
CLAUDE_MARKETING_MODEL=claude-sonnet-5

# ── VERCEL DEPLOYMENT (api/run.js) ────────────
# Required only when deploying marketing/ to Vercel. Protects the trigger endpoint from
# being called by strangers who'd otherwise spend your Claude API credits — generate a
# long random value (e.g. `openssl rand -hex 32`) and set it as a Vercel env var, then send
# it back as the `x-trigger-secret` request header on every call.
MARKETING_TRIGGER_SECRET=change-me-to-a-long-random-value

# ── LINKEDIN (pipeline/linkedin.js, publish.js --linkedin) ────
# From a LinkedIn developer app with the w_member_social scope (posting as your own
# profile) — organization/company-page posting needs LinkedIn's Community Management API
# partnership approval, which is a separate application process. See marketing/CLAUDE.md
# "Auto-posting to LinkedIn and TikTok" for the exact steps.
LINKEDIN_ACCESS_TOKEN=
LINKEDIN_AUTHOR_URN=urn:li:person:your-id-or-urn:li:organization:your-org-id

# ── TIKTOK (pipeline/tiktok.js, publish.js --tiktok) ──────────
# From a TikTok for Developers app with the video.publish scope. Until that app passes
# TikTok's Content Posting API audit, every post lands as a private SELF_ONLY draft in your
# own TikTok inbox, not published publicly — see marketing/CLAUDE.md before assuming this
# auto-publishes.
TIKTOK_ACCESS_TOKEN=

# ── ASOS BACKEND INTEGRATION ──────────────────
# Used by pipeline/leadsClient.js (DM Manager handoff) to authenticate against the real
# leads/contacts API. Must be a real user's credentials for the tenant that DSP leads
# should land in — there is no service-account/API-key auth in this backend.
MARKETING_API_BASE_URL=http://localhost:3000/api/v1
MARKETING_API_EMAIL=admin@demo-empresa.com
MARKETING_API_PASSWORD=change-me
