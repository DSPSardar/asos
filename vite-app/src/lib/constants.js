// src/lib/constants.js
//
// AI Agent Mastery enrolment fee — the only product since the 7-day bootcamp
// was sunset (2026-08). Mirrors backend/src/config/constants.js (the backend
// rejects a Won at any other amount) — keep the two in sync when the price
// changes.
export const ENROLMENT_FEE_PKR = 28000;
